[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.16.0"  # Usa la versión de Node que tengas
  GO_VERSION = ""           # Esto APAGA Go completamente

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
